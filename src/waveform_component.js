import Logger from "./logger";

export default class WaveformComponent {
  constructor(audioElement) {}

  init(element) {
    this.log.info("Loaded WaveformComponent");
  }

  static fillWaveform(audioBuffer) {}
  static handleClick() {}
}
